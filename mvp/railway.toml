# MVP Shopify app — Railway: Dockerfile path = mvp/Dockerfile, root directory = /
[build]
builder = "DOCKERFILE"
dockerfilePath = "mvp/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
