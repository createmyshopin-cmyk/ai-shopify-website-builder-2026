{% comment %}
  Full-width trust bar with top/bottom rules and inset vertical dividers between columns (desktop).
{% endcomment %}

{%- liquid
  assign icon_delivery = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13" rx="1"/><path d="M16 8h4l3 3v5h-7V8z"/><circle cx="5.5" cy="18.5" r="2.5"/><circle cx="18.5" cy="18.5" r="2.5"/></svg>'
  assign icon_payment = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="4" width="22" height="16" rx="2"/><path d="M1 10h22"/></svg>'
  assign icon_returns = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="1 4 1 10 7 10"/><path d="M3.51 15a9 9 0 1 0 .49-4.5"/></svg>'
  assign default_icons = icon_delivery | append: '||' | append: icon_payment | append: '||' | append: icon_returns
  assign default_icons_arr = default_icons | split: '||'

  if section.settings.border_color != blank
    assign rule = section.settings.border_color
  else
    assign rule = '#d4d4d4'
  endif

  if section.settings.divider_vertical_inset == blank
    assign divider_inset = 14
  else
    assign divider_inset = section.settings.divider_vertical_inset
  endif
-%}

{% style %}
  #shopify-section-{{ section.id }} {
    --etb-rule-color: {{ rule }};
    --etb-divider-inset: {{ divider_inset }}px;
  }
{% endstyle %}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div class="editorial-trust-bar section spacing-style color-{{ section.settings.color_scheme }}" style="{% render 'spacing-style', settings: section.settings %}">
  <div class="editorial-trust-bar__strip">
    <div class="editorial-trust-bar__inner">
      <div class="editorial-trust-bar__grid">
        {% for block in section.blocks %}
          {% if block.type == 'trust_item' %}
            {%- assign fallback_icon = default_icons_arr[forloop.index0] -%}
            <div class="etb-item" {{ block.shopify_attributes }}>
              <div class="etb-item__icon">
                {%- if block.settings.icon != blank -%}
                  {{ block.settings.icon }}
                {%- elsif fallback_icon != blank -%}
                  {{ fallback_icon }}
                {%- endif -%}
              </div>
              <div class="etb-item__text">
                {% if block.settings.heading != blank %}
                  <p class="etb-item__heading">{{ block.settings.heading }}</p>
                {% endif %}
                {% if block.settings.description != blank %}
                  <p class="etb-item__description">{{ block.settings.description }}</p>
                {% endif %}
              </div>
            </div>
          {% endif %}
        {% else %}
          <div class="etb-item">
            <div class="etb-item__icon">{{ icon_delivery }}</div>
            <div class="etb-item__text">
              <p class="etb-item__heading">Fast Delivery</p>
              <p class="etb-item__description">Free delivery from €125</p>
            </div>
          </div>
          <div class="etb-item">
            <div class="etb-item__icon">{{ icon_payment }}</div>
            <div class="etb-item__text">
              <p class="etb-item__heading">Buy Now Pay Later</p>
              <p class="etb-item__description">Pay within 30 days</p>
            </div>
          </div>
          <div class="etb-item">
            <div class="etb-item__icon">{{ icon_returns }}</div>
            <div class="etb-item__text">
              <p class="etb-item__heading">14 Day Returns</p>
              <p class="etb-item__description">Simple, straightforward returns</p>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>

{% stylesheet %}
  .editorial-trust-bar {
    width: 100%;
    overflow-x: clip;
  }

  /* Full viewport width bar with horizontal rules (reference: editorial trust strip) */
  .editorial-trust-bar__strip {
    box-sizing: border-box;
    width: 100%;
    border-top: 1px solid var(--etb-rule-color, rgba(0, 0, 0, 0.12));
    border-bottom: 1px solid var(--etb-rule-color, rgba(0, 0, 0, 0.12));
    background: var(--color-background);
  }

  @media screen and (min-width: 750px) {
    .editorial-trust-bar__strip {
      margin-inline: calc(50% - var(--viewport-width) / 2);
    }
  }

  .editorial-trust-bar__inner {
    margin: 0 auto;
    padding: clamp(20px, 3vw, 32px) var(--page-margin, 24px);
    max-width: none;
  }

  .editorial-trust-bar__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0;
    align-items: stretch;
  }

  .etb-item {
    position: relative;
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: clamp(12px, 2vw, 20px) clamp(16px, 3vw, 24px);
    box-sizing: border-box;
  }

  /* Inset vertical divider between columns (not before first column of each row) */
  @media screen and (min-width: 750px) {
    .etb-item:not(:nth-child(3n + 1))::before {
      content: '';
      position: absolute;
      left: 0;
      top: var(--etb-divider-inset, 14px);
      bottom: var(--etb-divider-inset, 14px);
      width: 1px;
      background: var(--etb-rule-color, rgba(0, 0, 0, 0.12));
    }
  }

  .etb-item__icon {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    color: var(--color-foreground);
    opacity: 0.85;
  }

  .etb-item__icon svg {
    width: 100%;
    height: 100%;
    display: block;
  }

  .etb-item__heading {
    margin: 0 0 6px;
    font-size: 0.8125rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-foreground-heading);
    line-height: 1.25;
  }

  .etb-item__description {
    margin: 0;
    font-size: 0.8125rem;
    font-weight: 400;
    color: var(--color-foreground);
    opacity: 0.72;
    line-height: 1.45;
  }

  @media screen and (max-width: 749px) {
    .editorial-trust-bar__inner {
      padding-inline: 16px;
    }

    .editorial-trust-bar__grid {
      grid-template-columns: 1fr;
    }

    .etb-item:not(:first-child) {
      border-top: 1px solid var(--etb-rule-color, rgba(0, 0, 0, 0.12));
    }

    .etb-item {
      padding: 20px 0;
    }
  }
{% endstylesheet %}

{% schema %}
  {
    "name": "Editorial Trust Bar",
    "tag": "section",
    "class": "section-wrapper",
    "disabled_on": {
      "groups": ["header", "footer"]
    },
    "settings": [
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "scheme-1",
        "info": "Background and text use the scheme; borders use the color below."
      },
      {
        "type": "color",
        "id": "border_color",
        "label": "Rule & divider color",
        "default": "#d4d4d4"
      },
      {
        "type": "range",
        "id": "divider_vertical_inset",
        "label": "Divider inset (desktop)",
        "min": 0,
        "max": 40,
        "step": 2,
        "unit": "px",
        "default": 14,
        "info": "Shortens vertical column lines so they do not touch the top and bottom borders."
      },
      {
        "type": "header",
        "content": "Padding"
      }, {
        "type": "range",
        "id": "padding-block-start",
        "label": "Top padding",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "default": 32
      }, {
        "type": "range",
        "id": "padding-block-end",
        "label": "Bottom padding",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "default": 32
      }
    ],
    "blocks": [
      {
        "type": "trust_item",
        "name": "Trust item",
        "limit": 6,
        "settings": [
          {
            "type": "html",
            "id": "icon",
            "label": "Icon SVG"
          }, {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Fast Delivery"
          }, {
            "type": "text",
            "id": "description",
            "label": "Description",
            "default": "Free delivery worldwide"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Editorial Trust Bar",
        "settings": {
          "border_color": "#d4d4d4",
          "divider_vertical_inset": 14
        },
        "blocks": [
          {
            "type": "trust_item",
            "settings": {
              "heading": "Fast Delivery Worldwide",
              "description": "Free delivery from €125"
            }
          }, {
            "type": "trust_item",
            "settings": {
              "heading": "Buy Now Pay Later",
              "description": "Pay within 30 days at home"
            }
          }, {
            "type": "trust_item",
            "settings": {
              "heading": "14 Day Returns",
              "description": "Simple, straightforward returns"
            }
          }
        ]
      }
    ]
  }
{% endschema %}