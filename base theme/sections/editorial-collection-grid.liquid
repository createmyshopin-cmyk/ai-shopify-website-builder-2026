<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="editorial-collection-grid spacing-style color-{{ section.settings.color_scheme }}"
  style="{% render 'spacing-style', settings: section.settings %}"
>
  {% if section.settings.heading != blank %}
    <div class="editorial-collection-grid__header">
      <h2 class="editorial-collection-grid__heading">{{ section.settings.heading }}</h2>
    </div>
  {% endif %}

  {% if section.blocks.size > 0 %}
    <div class="editorial-collection-grid__grid-bleed">
      <div class="editorial-collection-grid__grid" data-columns="{{ section.settings.columns }}">
      {% for block in section.blocks %}
        {% if block.type == 'collection_item' %}
          {% assign coll = block.settings.collection %}
          <a
            href="{{ coll.url | default: '/collections/all' }}"
            class="ecg-item"
            {{ block.shopify_attributes }}
          >
            <div class="ecg-item__media">
              {%- capture sizes -%}
                (min-width: 990px) calc(100vw / {{ section.settings.columns }}), 50vw
              {%- endcapture -%}
              {% if block.settings.image %}
                {{
                  block.settings.image
                  | image_url: width: 800
                  | image_tag:
                    class: 'ecg-item__img',
                    loading: 'lazy',
                    widths: '400, 600, 800',
                    sizes: sizes
                }}
              {% elsif coll != blank and coll.image %}
                {{
                  coll.image
                  | image_url: width: 800
                  | image_tag:
                    class: 'ecg-item__img',
                    loading: 'lazy',
                    widths: '400, 600, 800',
                    sizes: sizes
                }}
              {% else %}
                <div class="ecg-item__placeholder">
                  {{ 'collection-1' | placeholder_svg_tag: 'ecg-item__placeholder-svg' }}
                </div>
              {% endif %}
              <div class="ecg-item__overlay"></div>
            </div>
            <div class="ecg-item__label">
              <span>{{ block.settings.label | default: coll.title | default: 'Collection' }}</span>
            </div>
          </a>
        {% endif %}
      {% endfor %}
      </div>
    </div>
  {% endif %}
</div>

{% stylesheet %}
  .editorial-collection-grid {
    overflow-x: clip;
    width: 100%;
  }

  /* Full-bleed to align with grid; horizontal inset so heading is not flush to viewport edges. */
  .editorial-collection-grid__header {
    box-sizing: border-box;
    width: 100%;
    padding-inline: clamp(16px, 4vw, 36px);
    padding-bottom: 20px;
  }

  @media screen and (min-width: 750px) {
    .editorial-collection-grid__header {
      margin-inline: calc(50% - var(--viewport-width) / 2);
      padding-inline: clamp(20px, 5vw, 56px);
    }

    .editorial-collection-grid__grid-bleed {
      width: 100%;
      margin-inline: calc(50% - var(--viewport-width) / 2);
    }
  }

  .editorial-collection-grid__grid-bleed {
    width: 100%;
  }

  .editorial-collection-grid__heading {
    margin: 0;
    font-family: var(--font-heading--family, inherit);
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-foreground-heading);
  }

  .editorial-collection-grid__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 4px;
    width: 100%;
  }

  .editorial-collection-grid__grid[data-columns="3"] {
    grid-template-columns: repeat(3, 1fr);
  }

  .editorial-collection-grid__grid[data-columns="2"] {
    grid-template-columns: repeat(2, 1fr);
  }

  .ecg-item {
    position: relative;
    display: block;
    text-decoration: none;
    overflow: hidden;
    aspect-ratio: 3/4;
    border-radius: var(--style-border-radius-cards, 0px);
  }

  .ecg-item__media {
    position: absolute;
    inset: 0;
  }

  .ecg-item__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }

  .ecg-item__placeholder {
    width: 100%;
    height: 100%;
    background: var(--color-card-background, #d8d0c8);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ecg-item__placeholder-svg {
    width: 35%;
    height: auto;
    opacity: 0.15;
  }

  .ecg-item:hover .ecg-item__img {
    transform: scale(1.04);
  }

  .ecg-item__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.45) 0%, transparent 50%);
  }

  .ecg-item__label {
    position: absolute;
    bottom: 20px;
    left: 0;
    right: 0;
    text-align: center;
    z-index: 1;
  }

  .ecg-item__label span {
    display: inline-block;
    color: #ffffff;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  @media screen and (max-width: 989px) {
    .editorial-collection-grid__grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .editorial-collection-grid__grid[data-columns="3"] {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media screen and (max-width: 749px) {
    .editorial-collection-grid__header {
      padding-bottom: 16px;
    }

    .editorial-collection-grid__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2px;
    }
    .ecg-item {
      aspect-ratio: 1;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Editorial Collections",
  "tag": "section",
  "class": "section-wrapper",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Discover the Collections"
    },
    {
      "type": "select",
      "id": "columns",
      "label": "Columns (desktop)",
      "options": [
        { "value": "2", "label": "2 columns" },
        { "value": "3", "label": "3 columns" },
        { "value": "4", "label": "4 columns" }
      ],
      "default": "4"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 48
    }
  ],
  "blocks": [
    {
      "type": "collection_item",
      "name": "Collection",
      "limit": 6,
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Override image"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "info": "Defaults to collection title"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Editorial Collections",
      "blocks": [
        { "type": "collection_item", "settings": { "label": "Jackets" } },
        { "type": "collection_item", "settings": { "label": "Trousers" } },
        { "type": "collection_item", "settings": { "label": "Sweaters" } },
        { "type": "collection_item", "settings": { "label": "Footwear" } }
      ]
    }
  ]
}
{% endschema %}
